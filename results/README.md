# Results

Bulk outputs are ignored. Only compact, curated artifacts intended for the README or web application should be copied into `results/public/`.

Every public result should be traceable to:

- a configuration file;
- a random seed;
- a data-version manifest;
- a Git commit;
- a documented command.

